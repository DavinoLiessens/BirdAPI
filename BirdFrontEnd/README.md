# BirdFrontEnd

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 11.0.4.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

Hoe je dit in de toekomst gebruikt:
Voor development:

Voor aanpassingen maken en naar backend builden:

Voor het starten van je backend:
Nu kun je je .NET backend starten en deze zal automatisch de frontend serveren vanuit de ClientApp folder.

De baseUrl is nu ingesteld op http://localhost:5000, wat waarschijnlijk de poort is waarop je .NET backend draait. Als dit een andere poort is, kun je dit aanpassen in de environment bestanden.
